FIX APPLIED
===========

Issue: Invalid node group reference ["main"]
The actual node group name is dynamic: "${local.name_prefix}-nodes"
Which evaluates to: "vytalmed-prod-nodes"

Fixed Files:
------------
1. modules/envs/ai-services.tf (lines 39, 85)
   - eks_nodes_textract attachment
   - eks_nodes_bedrock attachment

2. modules/envs/kafka.tf (line 167)
   - eks_nodes_kafka attachment (bonus fix!)

Changed From:
  module.eks.eks_managed_node_groups["main"].iam_role_name

Changed To:
  module.eks.eks_managed_node_groups["${local.name_prefix}-nodes"].iam_role_name

Now Ready:
----------
terraform apply

Should succeed now!
