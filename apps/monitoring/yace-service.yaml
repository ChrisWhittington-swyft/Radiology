apiVersion: v1
kind: Service
metadata:
  name: yace-exporter
  namespace: monitoring
  labels:
    app: yace-exporter
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 5000
      targetPort: metrics
      protocol: TCP
  selector:
    app: yace-exporter
